The CSS for different pages get downloaded in the homepage itself
Different Components get loaded into the same page through routing
Tried implementing hooks in react.js to dynamically load the apps in the './react' but facing issues in fetching the url for images dynamically etc.
